<div class="container text-center">
      <h1 class="text-center">Administrator</h1>
  <p><%= link_to "Admin", admin_path %><%= " > #{params[:action].capitalize}" unless params[:action].eql?("index") %></p>
  <div class="btn-group">
    </div>
</div>